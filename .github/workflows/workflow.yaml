name: Run tests

on: push

jobs:
  run-tests:
    name: run-tests
    runs-on: ubuntu-latest
    steps:
      - name: Test with cargo
        uses: actions-rs/cargo@v1
        with:
          command: test
